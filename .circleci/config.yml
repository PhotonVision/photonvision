version: 2
jobs:
  build_ui:
    docker:
      - image: node:latest

    steps:

      - checkout
      
      - run: cd chameleon-client
      - run: npm install
      - run: npm run build
      
      - store_artifacts: 
          path: chameleon-client/dist/*

workflows:
  build:
    jobs:
      - build_ui
      