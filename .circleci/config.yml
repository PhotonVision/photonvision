version: 2 # use CircleCI 2.0
jobs:
  build_ui:
    docker:
      - image: node:latest

    steps:

      - checkout
      
      - run: cd chameleon-client
      - run: npm install
      - run: npm run build
      
      - store_artifacts: 
          path: chameleon-client/dist/*
      